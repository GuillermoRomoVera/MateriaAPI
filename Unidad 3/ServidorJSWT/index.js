const jwt = require('jsonwebtoken');

app.use(express.json)
app.post('Augin', (req,res,) {
    let
    res.json({token: })
});

app.get("/Alumnos", validorToken,) => {

});

function VerificarToken(req,res,) {
    req.
    JsonWebTokenError.VerificarToken(req.header)
    if (err) {
        res.json({error, "Token Invalido"});
    } else {

    });
}
    
}