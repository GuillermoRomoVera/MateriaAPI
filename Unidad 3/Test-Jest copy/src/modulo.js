function areaTriangulo(base, altura) {
    return (base, altura)/2;
  }

function numAleatorio(min, max) {
  return Math.random()*(max - min)+min;
}

module.exporte.areaTriangulo=areaTriangulo,
module.exporte.numAleatorio=numAleatorio;
